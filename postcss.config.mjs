// Tailwind is compiled by @tailwindcss/vite for the Vite entry point. Keeping
// this config explicit and empty prevents Vite 8 from trying to interpret the
// legacy Next/PostCSS plugin shape as a native PostCSS plugin.
export default { plugins: {} };
