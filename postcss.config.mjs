// Tailwind is compiled by @tailwindcss/vite for the Vite entry point. Keeping
// this config explicit and empty prevents Vite 8 from discovering an
// unrelated PostCSS plugin chain when Tailwind is already handled by Vite.
export default { plugins: {} };
